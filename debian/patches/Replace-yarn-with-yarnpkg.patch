From: Sophie Brun <sophie@kali.org>
Date: Thu, 24 Oct 2024 16:15:59 +0200
Subject: Replace yarn with yarnpkg

In Debian the command yarn is named yarnpkg
---
 cmd/ui/package.json                          |  4 ++--
 package.json                                 | 20 ++++++++++----------
 packages/go/stbernard/workspace/yarn/yarn.go |  8 ++++----
 3 files changed, 16 insertions(+), 16 deletions(-)
--- a/cmd/ui/package.json
+++ b/cmd/ui/package.json
@@ -7,10 +7,10 @@
         "debug": "vite --debug",
         "dev": "vite",
         "start": "vite",
-        "build": "yarn check-types && vite build",
+        "build": "yarnpkg check-types && vite build",
         "preview": "vite preview",
         "test": "TZ=America/Los_Angeles vitest",
-        "check-types": "yarn workspaces foreach -ptRv --include '{bh-shared-ui,js-client-library,doodle-ui}' run build && tsc --noEmit --pretty",
+        "check-types": "yarnpkg workspaces foreach -ptRv --include '{bh-shared-ui,js-client-library,doodle-ui}' run build && tsc --noEmit --pretty",
         "lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
         "format": "prettier --write \"src/**/*.@(js|jsx|ts|tsx|md|html|css|scss|json)\"",
         "check-format": "prettier  --list-different \"src/**/*.@(js|jsx|ts|tsx|md|html|css|scss|json)\""
@@ -112,7 +112,7 @@
         "vitest-canvas-mock": "^0.3.3",
         "whatwg-fetch": "^3.6.2"
     },
-    "packageManager": "yarn@3.5.0",
+    "packageManager": "yarnpkg@3.5.0",
     "msw": {
         "workerDirectory": "public"
     }
--- a/package.json
+++ b/package.json
@@ -8,18 +8,18 @@
     ]
   },
   "scripts": {
-    "dev": "yarn workspace bloodhound-ui dev",
-    "debug": "yarn workspace bloodhound-ui debug",
-    "start": "yarn workspace bloodhound-ui start",
-    "preview": "yarn workspace bloodhound-ui preview",
-    "build": "yarn workspace bloodhound-ui build",
-    "test": "yarn workspaces foreach -ptv run test run",
-    "check-types": "yarn workspaces foreach --exclude eslint-plugin-sx-props -ptv run check-types",
-    "lint": "yarn workspaces foreach --exclude eslint-plugin-sx-props -ptv run lint",
-    "format": "yarn workspaces foreach --exclude eslint-plugin-sx-props -ptv run format",
-    "check-format": "yarn workspaces foreach --exclude eslint-plugin-sx-props -ptv run check-format"
+    "dev": "yarnpkg workspace bloodhound-ui dev",
+    "debug": "yarnpkg workspace bloodhound-ui debug",
+    "start": "yarnpkg workspace bloodhound-ui start",
+    "preview": "yarnpkg workspace bloodhound-ui preview",
+    "build": "yarnpkg workspace bloodhound-ui build",
+    "test": "yarnpkg workspaces foreach -ptv run test run",
+    "check-types": "yarnpkg workspaces foreach --exclude eslint-plugin-sx-props -ptv run check-types",
+    "lint": "yarnpkg workspaces foreach --exclude eslint-plugin-sx-props -ptv run lint",
+    "format": "yarnpkg workspaces foreach --exclude eslint-plugin-sx-props -ptv run format",
+    "check-format": "yarnpkg workspaces foreach --exclude eslint-plugin-sx-props -ptv run check-format"
   },
-  "packageManager": "yarn@3.5.1",
+  "packageManager": "yarnpkg@3.5.1",
   "devDependencies": {
     "all-contributors-cli": "^6.26.1",
     "prettier": "^3.2.5",
--- a/packages/go/stbernard/workspace/yarn/yarn.go
+++ b/packages/go/stbernard/workspace/yarn/yarn.go
@@ -71,7 +71,7 @@
 			Env:     env.Slice(),
 		}
 		if _, err := cmdrunner.Run(context.TODO(), executionPlan); err != nil {
-			return fmt.Errorf("yarn install at %v: %w", path, err)
+			return fmt.Errorf("yarnpkg install at %v: %w", path, err)
 		}
 	}
 
@@ -92,7 +92,7 @@
 		Env:     env.Slice(),
 	}
 	if _, err := cmdrunner.Run(context.TODO(), executionPlan); err != nil {
-		return fmt.Errorf("running yarn format: %w", err)
+		return fmt.Errorf("running yarnpkg format: %w", err)
 	} else {
 		return nil
 	}
@@ -112,7 +112,7 @@
 		Env:     env.Slice(),
 	}
 	if _, err := cmdrunner.Run(context.TODO(), executionPlan); err != nil {
-		return fmt.Errorf("yarn build at %v: %w", cwd, err)
+		return fmt.Errorf("yarnpkg build at %v: %w", cwd, err)
 	} else {
 		return nil
 	}
@@ -132,7 +132,7 @@
 		Env:     env.Slice(),
 	}
 	if _, err := cmdrunner.Run(context.TODO(), executionPlan); err != nil {
-		return fmt.Errorf("yarn test at %v: %w", cwd, err)
+		return fmt.Errorf("yarnpkg test at %v: %w", cwd, err)
 	} else {
 		return nil
 	}
